<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>URL Shortener</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>

    <div id="server-flash"
      data-errors='<%= JSON.stringify(flashErrors || []) %>'
      data-success='<%= JSON.stringify(flashSuccess || []) %>'>
    </div>

    <header>
      <nav class="navbar">
        <h2 class="logo">URL-Shortner</h2>
        <a href="/admin/loginPage" class="nav-link">See URL Database</a>
      </nav>
    </header>
